# Data Directory

This directory contains runtime data for ResumeRank AI:

- `resume_screener.db` - SQLite database (auto-created on first run)
- `uploads/` - Uploaded resume PDFs organized by session ID

**Note:** All files in this directory (except this README) are gitignored.
